---
description: 推送前必须通过 CI；禁止在 CI 失败时 push
alwaysApply: true
---

# CI 门禁

## 推送前

1. `pnpm install` 会自动启用 pre-push 钩子；**每次 `git push` 前自动运行 `pnpm test:ci`**。
2. 也可手动运行 `bash scripts/ci-local.sh` 预检。
3. **禁止**在 GitHub Actions CI 失败时 push 到 `main`/`master`（除非明确使用 `--no-verify` 且已知风险）。

## CI 覆盖范围

与 `.github/workflows/ci.yml` 一致：`cargo test --lib`、前端 `tsc` / `lint` / `build`、Playwright `ui-mock` E2E。

## 依赖锁定

`frontend/pnpm-lock.yaml` 必须纳入版本控制；修改 `package.json` 后需更新 lockfile 并一并提交。
