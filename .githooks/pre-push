#!/usr/bin/env bash
# push 前自动执行完整 CI（Mac + Docker Linux）。
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"

echo ""
echo "▶ pre-push: 运行完整 CI（test:ci + test:ci:linux）…"
echo "  预计 5–20 分钟（含 Docker 编译）"
echo "  跳过检查请使用: git push --no-verify"
echo ""

exec "$ROOT/scripts/ci-pre-push.sh"
