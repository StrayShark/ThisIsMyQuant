---
description: 推送前必须通过 CI；禁止在 CI 失败时 push
alwaysApply: true
---

# CI 门禁

## 推送前

1. `pnpm install` 会自动启用 pre-push 钩子。
2. **每次 `git push` 前自动依次运行**：
   - `pnpm test:ci` — Mac 本地（frontend + e2e + Rust）
   - `pnpm test:ci:linux` — Docker Ubuntu（Tauri 系统库 + `cargo test --lib`）
3. 手动预检等价命令：`pnpm test:ci:all`
4. **禁止**在任一检查或 GitHub Actions 失败时 push（除非 `--no-verify` 且已知风险）。

## 前置

- Docker Desktop 须已启动（pre-push 第二步需要）
- E2E 首次需：`pnpm --dir frontend exec playwright install chromium`

## 依赖锁定

`frontend/pnpm-lock.yaml` 必须纳入版本控制；修改 `package.json` 后需更新 lockfile 并一并提交。
